---
import Base from '../layouts/Base.astro'
---

<Base>
  <div id="mock-root" style="text-align:center;padding:40px 20px;color:var(--muted);font-size:14px">
    Загрузка...
  </div>
</Base>

<script type="module">
  import { createRoot } from 'react-dom/client'
  import { createElement } from 'react'
  import { MockInterview } from '../MockInterview.tsx'

  const root = createRoot(document.getElementById('mock-root'))
  root.render(createElement(MockInterview))
</script>
