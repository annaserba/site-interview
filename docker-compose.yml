services:
  app:
    build: .
    container_name: interview-app
    ports:
      - "80:80"
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
