---
id: release-vs-deploy
title: Чем релиз отличается от деплоя?
aliases: []
category: Delivery
scope: universal
languages: []
roles: ["DevOps","Backend","SRE"]
companies: ["Avito"]
level: Middle
stage: Техническое
tags: ["Release", "Deployment", "DevOps", "Process"]
duration: 10 мин
difficulty: 2
sourceType: video
sourceUrl: ""
sourceVideos: [{"company":"Avito","url":"https://www.youtube.com/watch?v=R23FjSp8Z-A","title":"Что такое релизы? Feature Flags, Canary и Rollback"}]
---

## Короткий ответ

Деплой (deployment) — техническая операция: выкатка новой версии кода на сервер. Релиз (release) — бизнес-событие: новая функциональность стала доступна пользователям. С фича-флагами эти понятия разделяются: код можно задеплоить, но не релизить. Обратное тоже верно: можно релизить функциональность без деплоя (переключение флага).

## Контекст

Интервьюер проверяет понимание современной инженерии доставки. Ключевой инсайт: раньше деплой = релиз (выкатил код → пользователи увидели). Сейчас это два независимых процесса, что позволяет безопаснее доставлять изменения.

## Как строить ответ

### Деплой
Деплой — это перемещение артефакта (кода, контейнера) на целевую среду:
- Замена бинарников на сервере
- Обновление контейнеров в Kubernetes
- Копирование статики на CDN
- Деплой может происходить много раз в день (continuous deployment)

### Релиз
Релиз — это момент, когда пользователи получают доступ к новой функциональности:
- Включение фича-флага
- Изменение конфигурации балансировщика
- Апдейт магазина приложений
- Релиз — бизнес-решение, а не техническое

### Почему это важно
1. **Trunk-based development** — все коммитят в main, код сразу деплоится, но скрыт флагами
2. **Безопасность** — деплой можно делать в любое время, релиз — в рабочее с поддержкой
3. **Decoupling** — разработчики деплоят код, продукт-менеджеры решают когда релизить
4. **Rollback** — при проблемах выключается флаг, а не откатывается деплой

### Реальные примеры
- Facebook деплоит сотни раз в день, но релизит фичи по готовности
- Google Chrome: канареечные сборки деплоятся автоматически, релиз — ручной
- Netflix: continuous deployment + feature flags для экспериментов

## Пример ответа

Раньше эти понятия были синонимами: выкатил код на прод — пользователи увидели новую фичу. Сейчас, благодаря фича-флагам, это разные процессы.

У нас в команде деплой происходит автоматически при каждом merge в main. CI/CD пайплайн собирает контейнер, прогоняет тесты и выкатывает на staging. Если staging-тесты зелёные — контейнер летит на прод. Это занимает 15-20 минут. Но пользователи новую функциональность не видят — она скрыта за флагом.

Релиз — это когда продакт-менеджер принимает решение включить фичу. Мы включаем флаг сначала на 5% пользователей, мониторим метрики, через пару часов раскатываем до 100%. Если что-то пошло не так — выключаем флаг за секунду, без отката деплоя.

Ключевой инсайт: деплой — техническая операция (может происходить десятки раз в день), релиз — бизнес-решение (происходит когда фича готова и проверена).

## Частые ошибки

- Использовать термины как синонимы
- Релизить без фича-флагов — нельзя быстро откатить
- Деплоить без мониторинга — не узнаешь о проблемах до релиза

## Дополнительные вопросы

- Как организовать деплой в пятницу вечером?
- Что такое dark launch и зачем он нужен?
- Чем отличается canary deploy от canary release?
